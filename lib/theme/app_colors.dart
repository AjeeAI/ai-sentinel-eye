import 'package:flutter/material.dart';

class AppColors {
  // --- Backgrounds & Surfaces ---
  static const Color background = Color(0xFF121212); // Deep Black
  static const Color surface = Color(0xFF1E1E1E);    // Elevated Dark Gray
  static const Color surfaceLight = Color(0xFF2A2A2A); // Lighter Card Gray

  // --- Brand Accents ---
  static const Color primary = Color(0xFF00FF41);    // Matrix Green
  
  // --- Status & Severity ---
  static const Color critical = Color(0xFFFF453A);   // Red
  static const Color warning = Color(0xFFFF9F0A);    // Orange
  static const Color safe = Color(0xFF00FF41);       // Green

  // --- Text Colors ---
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.white38;
}